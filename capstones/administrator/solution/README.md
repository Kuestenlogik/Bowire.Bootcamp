# Administrator capstone — reference solution

What's here:

| File | What it is |
| --- | --- |
| `RUNBOOK.md` | The five-section ops runbook the capstone asks for (day-1 + four day-2 procedures). |
| `smoke-test.sh` | The eight-check smoke test, runnable from outside the host. |

The compose + k8s starters live under [`../compose/`](../compose/) and [`../k8s/`](../k8s/); their `# GAP:` markers are *not* filled in by the reference solution (each org's OIDC issuer + hostname is different — the runbook + smoke test describe the contract, not the values).

---

**Back to:** [Administrator capstone](../README.md)
