# User capstone — "Diagnose a flaky API endpoint" runbook

> **Status: scaffold (PR 1 — structure only).** Full scenario text, sample workspace, and reference runbook land in PR 3 per the [v2.1 + purpose-paths audit](../../audits/2026-06-30-v21-and-purpose-paths-audit.md), Section 3.

**Deliverable (per audit):** a `.bww` workspace file + a runbook `README.md`. The runbook
walks a realistic mixed-protocol scenario (REST checkout endpoint + gRPC
inventory sub-call + WebSocket order-status stream); the workspace pins the
source URLs, recorded reproductions, mock-server config, AI-agent MCP config,
and notes captured during the diagnosis.

TODO PR-3: write the scenario brief, ship the starter `.bww`, ship the reference completed runbook.
